{
  "name": "refactorthis",
  "version": "1.0.0",
  "description": "This is comprised of badly written code and some unit tests. The overall objective is to refactor the code and keep the tests passing, however there are actually no rules here so if you want to change the tests thats also fine.",
  "scripts": {
    "test": "dotnet test",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.4"
  },
  "lint-staged": {
    "*.cs": "dotnet format RefactorThis.sln --fix-whitespace --fix-style warn --include"
  }
}
